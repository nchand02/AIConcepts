---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="About This Course - AI Concepts">
  <div class="about-container">
    <div class="about-hero">
      <h1>Who This Course Is For</h1>
      <p class="hero-subtitle">A practical AI education for technical leaders and decision-makers</p>
    </div>

    <section class="about-section">
      <div class="section-icon">🎯</div>
      <h2>Target Audience</h2>
      <div class="audience-grid">
        <div class="audience-card">
          <div class="audience-icon">💼</div>
          <h3>CTOs</h3>
          <p>Making strategic AI adoption decisions for your organization</p>
        </div>
        <div class="audience-card">
          <div class="audience-icon">🏢</div>
          <h3>Technical VPs</h3>
          <p>Evaluating AI vendors and understanding solution architectures</p>
        </div>
        <div class="audience-card">
          <div class="audience-icon">👥</div>
          <h3>Engineering Managers</h3>
          <p>Understanding LLMs to guide your team's AI integration efforts</p>
        </div>
        <div class="audience-card">
          <div class="audience-icon">🔧</div>
          <h3>Tech Leads</h3>
          <p>Building AI capabilities into your products and services</p>
        </div>
      </div>
    </section>

    <section class="about-section">
      <div class="section-icon">⏱️</div>
      <h2>What You Get</h2>
      <div class="benefits-list">
        <div class="benefit-card">
          <div class="benefit-icon">✅</div>
          <div class="benefit-content">
            <h3>Plain-English Explanations</h3>
            <p>No fluff, no jargon overload. Complex AI concepts broken down into clear, actionable insights.</p>
          </div>
        </div>
        <div class="benefit-card">
          <div class="benefit-icon">✅</div>
          <div class="benefit-content">
            <h3>Real Enterprise Context</h3>
            <p>Not toy examples. Real-world scenarios, production considerations, and enterprise-scale challenges.</p>
          </div>
        </div>
        <div class="benefit-card">
          <div class="benefit-icon">✅</div>
          <div class="benefit-content">
            <h3>Decision Frameworks</h3>
            <p>Ready-to-use frameworks you can apply immediately to evaluate technologies and make informed decisions.</p>
          </div>
        </div>
        <div class="benefit-card">
          <div class="benefit-icon">✅</div>
          <div class="benefit-content">
            <h3>Reliable Sources</h3>
            <p>Every claim cited. Built on peer-reviewed research, official documentation, and industry best practices.</p>
          </div>
        </div>
      </div>
    </section>

    <section class="about-section">
      <div class="section-icon">📚</div>
      <h2>Course Structure</h2>
      <p class="section-description">Our comprehensive curriculum covers 15 chapters, taking you from AI foundations to advanced system design:</p>
      <div class="structure-grid">
        <div class="structure-card">
          <div class="structure-number">01-04</div>
          <h3>Foundations</h3>
          <p>Core concepts, machine learning basics, deep learning, and transformer architecture</p>
        </div>
        <div class="structure-card">
          <div class="structure-number">05-07</div>
          <h3>LLMs & Generative AI</h3>
          <p>Large language models, generative AI techniques, and vector embeddings</p>
        </div>
        <div class="structure-card">
          <div class="structure-number">08-10</div>
          <h3>Advanced Techniques</h3>
          <p>RAG systems, fine-tuning strategies, and multimodal AI applications</p>
        </div>
        <div class="structure-card">
          <div class="structure-number">11-14</div>
          <h3>Production & Scale</h3>
          <p>AI agents, tooling ecosystem, infrastructure, and system design patterns</p>
        </div>
      </div>
    </section>

    <section class="about-section cta-section">
      <h2>Ready to Get Started?</h2>
      <p>Begin your journey from AI fundamentals to production-ready systems.</p>
      <div class="cta-buttons">
        <a href="/AIConcepts/chapters/01-foundations/" class="btn btn-primary">
          <span>Start Learning</span>
          <span>→</span>
        </a>
        <a href="/AIConcepts/#chapters" class="btn btn-secondary">
          <span>Browse Chapters</span>
        </a>
      </div>
    </section>
  </div>
</BaseLayout>

<style>
  .about-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: var(--space-2xl) var(--space-xl);
  }

  .about-hero {
    text-align: center;
    margin-bottom: var(--space-4xl);
    padding: var(--space-4xl) var(--space-xl);
    background: var(--gradient-mesh);
    border-radius: var(--radius-2xl);
    position: relative;
    overflow: hidden;
  }

  .about-hero::before {
    content: '';
    position: absolute;
    inset: 0;
    background: var(--glass-bg);
    backdrop-filter: blur(20px);
    z-index: 0;
  }

  .about-hero h1 {
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    margin-bottom: var(--space-lg);
    background: var(--gradient-primary);
    background-size: 200% 200%;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    animation: gradient-flow 6s ease infinite;
    font-weight: 800;
    letter-spacing: -0.03em;
    line-height: 1.1;
    position: relative;
    z-index: 1;
  }

  .hero-subtitle {
    font-size: clamp(1.125rem, 2vw, 1.5rem);
    color: var(--color-text-secondary);
    font-weight: 600;
    position: relative;
    z-index: 1;
  }

  @keyframes gradient-flow {
    0%, 100% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
  }

  .about-section {
    margin-bottom: var(--space-4xl);
    padding: var(--space-3xl) 0;
  }

  .section-icon {
    font-size: 3rem;
    text-align: center;
    margin-bottom: var(--space-lg);
    animation: float 3s ease-in-out infinite;
  }

  @keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-10px); }
  }

  .about-section h2 {
    text-align: center;
    font-size: clamp(2rem, 4vw, 2.5rem);
    margin-bottom: var(--space-xl);
    font-weight: 700;
  }

  .section-description {
    text-align: center;
    font-size: var(--font-size-lg);
    color: var(--color-text-secondary);
    max-width: 800px;
    margin: 0 auto var(--space-2xl);
    line-height: var(--line-height-relaxed);
  }

  .audience-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: var(--space-xl);
  }

  .audience-card {
    background: var(--glass-bg);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border: 1px solid var(--glass-border);
    border-radius: var(--radius-xl);
    padding: var(--space-2xl);
    text-align: center;
    transition: all var(--transition-spring);
    position: relative;
    overflow: hidden;
  }

  .audience-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: var(--gradient-primary);
    transform: scaleX(0);
    transition: transform var(--transition-spring);
  }

  .audience-card:hover {
    transform: translateY(-8px) scale(1.02);
    box-shadow: var(--shadow-glow);
    border-color: rgba(139, 92, 246, 0.5);
  }

  .audience-card:hover::before {
    transform: scaleX(1);
  }

  .audience-icon {
    font-size: 3rem;
    margin-bottom: var(--space-lg);
    transition: transform var(--transition-spring);
  }

  .audience-card:hover .audience-icon {
    transform: scale(1.2) rotate(5deg);
  }

  .audience-card h3 {
    font-size: var(--font-size-xl);
    margin-bottom: var(--space-md);
    font-weight: 700;
    color: var(--color-text-primary);
  }

  .audience-card p {
    color: var(--color-text-secondary);
    line-height: var(--line-height-relaxed);
  }

  .benefits-list {
    display: flex;
    flex-direction: column;
    gap: var(--space-xl);
    max-width: 900px;
    margin: 0 auto;
  }

  .benefit-card {
    display: flex;
    gap: var(--space-xl);
    background: var(--glass-bg);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border: 1px solid var(--glass-border);
    border-radius: var(--radius-xl);
    padding: var(--space-2xl);
    transition: all var(--transition-spring);
    position: relative;
  }

  .benefit-card::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0;
    bottom: 0;
    width: 4px;
    background: var(--gradient-primary);
    border-radius: var(--radius-xl) 0 0 var(--radius-xl);
    transform: scaleY(0);
    transition: transform var(--transition-spring);
  }

  .benefit-card:hover {
    transform: translateX(8px);
    box-shadow: var(--shadow-lg);
    border-color: rgba(139, 92, 246, 0.5);
  }

  .benefit-card:hover::before {
    transform: scaleY(1);
  }

  .benefit-icon {
    font-size: 2.5rem;
    flex-shrink: 0;
    width: 60px;
    height: 60px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, rgba(139, 92, 246, 0.1), rgba(99, 102, 241, 0.1));
    border-radius: var(--radius-lg);
    transition: transform var(--transition-spring);
  }

  .benefit-card:hover .benefit-icon {
    transform: scale(1.1) rotate(-5deg);
  }

  .benefit-content h3 {
    font-size: var(--font-size-xl);
    margin-bottom: var(--space-sm);
    font-weight: 700;
    color: var(--color-text-primary);
  }

  .benefit-content p {
    color: var(--color-text-secondary);
    line-height: var(--line-height-relaxed);
  }

  .structure-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: var(--space-xl);
  }

  .structure-card {
    background: var(--glass-bg);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border: 1px solid var(--glass-border);
    border-radius: var(--radius-xl);
    padding: var(--space-2xl);
    text-align: center;
    transition: all var(--transition-spring);
  }

  .structure-card:hover {
    transform: translateY(-5px);
    box-shadow: var(--shadow-lg);
    border-color: rgba(139, 92, 246, 0.5);
  }

  .structure-number {
    display: inline-block;
    padding: var(--space-sm) var(--space-lg);
    background: var(--gradient-primary);
    color: white;
    font-weight: 700;
    font-size: var(--font-size-lg);
    border-radius: var(--radius-full);
    margin-bottom: var(--space-lg);
    transition: transform var(--transition-spring);
  }

  .structure-card:hover .structure-number {
    transform: scale(1.1);
  }

  .structure-card h3 {
    font-size: var(--font-size-xl);
    margin-bottom: var(--space-md);
    font-weight: 700;
    color: var(--color-text-primary);
  }

  .structure-card p {
    color: var(--color-text-secondary);
    line-height: var(--line-height-relaxed);
  }

  .cta-section {
    text-align: center;
    padding: var(--space-4xl) var(--space-xl);
    background: var(--gradient-mesh);
    border-radius: var(--radius-2xl);
    position: relative;
    overflow: hidden;
  }

  .cta-section::before {
    content: '';
    position: absolute;
    inset: 0;
    background: var(--glass-bg);
    backdrop-filter: blur(20px);
    z-index: 0;
  }

  .cta-section h2 {
    position: relative;
    z-index: 1;
    margin-bottom: var(--space-lg);
  }

  .cta-section > p {
    position: relative;
    z-index: 1;
    font-size: var(--font-size-lg);
    color: var(--color-text-secondary);
    margin-bottom: var(--space-2xl);
  }

  .cta-buttons {
    display: flex;
    gap: var(--space-lg);
    justify-content: center;
    flex-wrap: wrap;
    position: relative;
    z-index: 1;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-sm);
    padding: var(--space-lg) var(--space-2xl);
    border-radius: var(--radius-full);
    font-weight: 600;
    font-size: var(--font-size-lg);
    text-decoration: none;
    transition: all var(--transition-spring);
    position: relative;
    overflow: hidden;
    border: none;
  }

  .btn > * {
    position: relative;
    z-index: 2;
  }

  .btn::before {
    content: '';
    position: absolute;
    inset: 0;
    background: var(--gradient-primary);
    transition: transform var(--transition-base);
    z-index: 0;
  }

  .btn::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(255,255,255,0.2) 0%, transparent 100%);
    opacity: 0;
    transition: opacity var(--transition-base);
    z-index: 1;
  }

  .btn-primary {
    background: var(--gradient-primary);
    color: white;
    box-shadow: var(--shadow-colored);
  }

  .btn-primary:hover {
    text-decoration: none;
    transform: translateY(-3px) scale(1.02);
    box-shadow: var(--shadow-glow), var(--shadow-xl);
  }

  .btn-primary:hover::after {
    opacity: 1;
  }

  .btn-secondary {
    background: var(--glass-bg);
    backdrop-filter: blur(10px);
    color: var(--color-text-primary);
    border: 2px solid var(--color-border);
  }

  .btn-secondary::before {
    background: var(--gradient-primary);
    opacity: 0;
  }

  .btn-secondary:hover {
    border-color: transparent;
    color: white;
    text-decoration: none;
    transform: translateY(-3px) scale(1.02);
    box-shadow: var(--shadow-colored);
  }

  .btn-secondary:hover::before {
    opacity: 1;
  }

  @media (max-width: 768px) {
    .about-container {
      padding: var(--space-xl) var(--space-md);
    }

    .about-hero {
      padding: var(--space-2xl) var(--space-lg);
    }

    .audience-grid,
    .structure-grid {
      grid-template-columns: 1fr;
    }

    .benefit-card {
      flex-direction: column;
      text-align: center;
    }

    .benefit-icon {
      margin: 0 auto;
    }

    .cta-buttons {
      flex-direction: column;
      align-items: stretch;
    }
  }
</style>
