---
import BaseLayout from '../layouts/BaseLayout.astro';
const base = import.meta.env.BASE_URL;
---

<BaseLayout title="Home" description="Comprehensive guide to Artificial Intelligence and Machine Learning - from foundations to advanced applications" currentPage="">
  <div class="hero">
    <h1>🤖 AI Learning Portal</h1>
    <p class="hero-subtitle">A Comprehensive Guide to Artificial Intelligence & Machine Learning</p>
    <p class="hero-description">
      Master the fundamentals and advanced concepts of AI, from basic machine learning to cutting-edge 
      large language models, multimodal systems, and AI agents. This interactive learning portal covers 
      everything you need to understand and build with AI.
    </p>
    
    <div class="cta-buttons">
      <a href={`${base.endsWith("/") ? base : base + "/"}chapters/01-foundations`} class="btn btn-primary">Start Learning →</a>
    </div>
  </div>

  <div class="features-grid">
    <div class="feature-card">
      <div class="feature-icon">📚</div>
      <h3>Comprehensive Content</h3>
      <p>15 detailed chapters covering foundations through advanced topics like RAG, fine-tuning, and AI agents.</p>
    </div>
    
    <div class="feature-card">
      <div class="feature-icon">📊</div>
      <h3>Visual Learning</h3>
      <p>Rich diagrams, flowcharts, and visual aids using Mermaid.js and ASCII art for better understanding.</p>
    </div>
    
    <div class="feature-card">
      <div class="feature-icon">💡</div>
      <h3>Practical Examples</h3>
      <p>Real-world code examples, exercises, and hands-on projects to reinforce learning.</p>
    </div>
    
    <div class="feature-card">
      <div class="feature-icon">🌓</div>
      <h3>Dark Mode</h3>
      <p>Comfortable reading experience with automatic theme switching and preference persistence.</p>
    </div>
    
    <div class="feature-card">
      <div class="feature-icon">📱</div>
      <h3>Responsive Design</h3>
      <p>Fully optimized for mobile, tablet, and desktop viewing with smooth navigation.</p>
    </div>
    
    <div class="feature-card">
      <div class="feature-icon">🔍</div>
      <h3>Searchable</h3>
      <p>Quick search functionality to find topics and concepts across all chapters instantly.</p>
    </div>
  </div>

  <section class="chapter-overview">
    <h2>📖 What You'll Learn</h2>
    
    <div class="chapter-list">
      <div class="chapter-item">
        <div class="chapter-num">01</div>
        <div class="chapter-info">
          <h3>Foundations of AI</h3>
          <p>History, core concepts, search algorithms, knowledge representation, and the evolution of artificial intelligence.</p>
          <a href={`${base.endsWith("/") ? base : base + "/"}chapters/01-foundations`} class="chapter-link">Explore →</a>
        </div>
      </div>
      
      <div class="chapter-item">
        <div class="chapter-num">02</div>
        <div class="chapter-info">
          <h3>Machine Learning</h3>
          <p>Supervised, unsupervised, and reinforcement learning fundamentals with practical algorithms and applications.</p>
          <a href={`${base.endsWith("/") ? base : base + "/"}chapters/02-machine-learning`} class="chapter-link">Explore →</a>
        </div>
      </div>
      
      <div class="chapter-item">
        <div class="chapter-num">03</div>
        <div class="chapter-info">
          <h3>Deep Learning</h3>
          <p>Neural networks, CNNs, RNNs, LSTMs, and the foundations of modern deep learning architectures.</p>
          <a href={`${base.endsWith("/") ? base : base + "/"}chapters/03-deep-learning`} class="chapter-link">Explore →</a>
        </div>
      </div>
      
      <div class="chapter-item">
        <div class="chapter-num">04</div>
        <div class="chapter-info">
          <h3>Transformers</h3>
          <p>Attention mechanisms, the transformer architecture, BERT, GPT, and the revolution in NLP.</p>
          <a href={`${base.endsWith("/") ? base : base + "/"}chapters/04-transformers`} class="chapter-link">Explore →</a>
        </div>
      </div>
      
      <div class="chapter-item">
        <div class="chapter-num">05</div>
        <div class="chapter-info">
          <h3>Large Language Models</h3>
          <p>GPT-4, Claude, LLaMA, training at scale, prompting strategies, and LLM capabilities.</p>
          <a href={`${base.endsWith("/") ? base : base + "/"}chapters/05-llms`} class="chapter-link">Explore →</a>
        </div>
      </div>
      
      <div class="chapter-item">
        <div class="chapter-num">06</div>
        <div class="chapter-info">
          <h3>Generative AI</h3>
          <p>Diffusion models, GANs, VAEs, DALL-E, Stable Diffusion, and creative AI applications.</p>
          <a href={`${base.endsWith("/") ? base : base + "/"}chapters/06-generative-ai`} class="chapter-link">Explore →</a>
        </div>
      </div>
      
      <div class="chapter-item">
        <div class="chapter-num">07</div>
        <div class="chapter-info">
          <h3>Embeddings & Vector Databases</h3>
          <p>Semantic search, vector representations, Pinecone, Weaviate, and similarity search at scale.</p>
          <a href={`${base.endsWith("/") ? base : base + "/"}chapters/07-embeddings`} class="chapter-link">Explore →</a>
        </div>
      </div>
      
      <div class="chapter-item">
        <div class="chapter-num">08</div>
        <div class="chapter-info">
          <h3>Retrieval-Augmented Generation</h3>
          <p>Combining retrieval and generation, building RAG systems, and grounding LLMs in real data.</p>
          <a href={`${base.endsWith("/") ? base : base + "/"}chapters/08-rag`} class="chapter-link">Explore →</a>
        </div>
      </div>
      
      <div class="chapter-item">
        <div class="chapter-num">09</div>
        <div class="chapter-info">
          <h3>Fine-Tuning</h3>
          <p>LoRA, QLoRA, PEFT methods, instruction tuning, and adapting models for specific tasks.</p>
          <a href={`${base.endsWith("/") ? base : base + "/"}chapters/09-fine-tuning`} class="chapter-link">Explore →</a>
        </div>
      </div>
      
      <div class="chapter-item">
        <div class="chapter-num">10</div>
        <div class="chapter-info">
          <h3>Multimodal AI</h3>
          <p>Vision-language models, GPT-4V, Gemini, CLIP, and AI systems that understand multiple modalities.</p>
          <a href={`${base.endsWith("/") ? base : base + "/"}chapters/10-multimodal`} class="chapter-link">Explore →</a>
        </div>
      </div>
      
      <div class="chapter-item">
        <div class="chapter-num">11</div>
        <div class="chapter-info">
          <h3>AI Agents & MCP</h3>
          <p>Autonomous agents, ReAct, AutoGPT, Model Context Protocol, and agentic AI systems.</p>
          <a href={`${base.endsWith("/") ? base : base + "/"}chapters/11-agents`} class="chapter-link">Explore →</a>
        </div>
      </div>
      
      <div class="chapter-item">
        <div class="chapter-num">12</div>
        <div class="chapter-info">
          <h3>AI Tooling & Ecosystems</h3>
          <p>LangChain, LlamaIndex, Hugging Face, development frameworks, and the AI tech stack.</p>
          <a href={`${base.endsWith("/") ? base : base + "/"}chapters/12-tooling`} class="chapter-link">Explore →</a>
        </div>
      </div>
      
      <div class="chapter-item">
        <div class="chapter-num">13</div>
        <div class="chapter-info">
          <h3>AI Infrastructure</h3>
          <p>Model serving, inference optimization, quantization, distributed training, and production deployment.</p>
          <a href={`${base.endsWith("/") ? base : base + "/"}chapters/13-infrastructure`} class="chapter-link">Explore →</a>
        </div>
      </div>
      
      <div class="chapter-item">
        <div class="chapter-num">14</div>
        <div class="chapter-info">
          <h3>System Design for AI</h3>
          <p>Architecture patterns, scaling strategies, monitoring, and building production AI systems.</p>
          <a href={`${base.endsWith("/") ? base : base + "/"}chapters/14-system-design`} class="chapter-link">Explore →</a>
        </div>
      </div>
      
      <div class="chapter-item">
        <div class="chapter-num">15</div>
        <div class="chapter-info">
          <h3>Complete Glossary</h3>
          <p>Comprehensive reference of all AI/ML terms, concepts, and terminology used throughout the portal.</p>
          <a href={`${base.endsWith("/") ? base : base + "/"}chapters/15-glossary`} class="chapter-link">Explore →</a>
        </div>
      </div>
    </div>
  </section>

  <section class="learning-path">
    <h2>🎯 Recommended Learning Path</h2>
    <p class="section-subtitle">Follow this structured progression through AI concepts from fundamentals to advanced applications</p>
    
    <div class="diagram-container">
      <pre class="mermaid" role="img" aria-label="AI Learning Path: A flowchart showing the recommended progression through AI topics from foundations to production systems" set:html={`graph TD
    %% Foundation Layer
    A[Foundations of AI] --> B[Machine Learning]
    B --> C[Deep Learning]
    
    %% Core Architecture Layer
    C --> D[Transformers]
    D --> E[Large Language Models]
    
    %% Specialization Paths
    E --> F[Choose Specialization]
    F -->|Creative Applications| G[Generative AI]
    F -->|Knowledge Systems| H[RAG & Embeddings]
    F -->|Custom Models| I[Fine-Tuning]
    
    %% Advanced Integration
    G --> J[Multimodal AI]
    H --> J
    I --> J
    
    %% Production & Reference
    J --> K[AI Agents & MCP]
    K --> L[System Design]
    L --> M[Tooling & Infrastructure]
    M --> N[Glossary Reference]
    
    %% Semantic Styling - Foundation (Blue)
    classDef foundation fill:#dbeafe,stroke:#3b82f6,stroke-width:2px,color:#1e293b
    classDef core fill:#ede9fe,stroke:#8b5cf6,stroke-width:2px,color:#1e293b
    classDef specialization fill:#fef3c7,stroke:#f59e0b,stroke-width:2px,color:#1e293b
    classDef advanced fill:#fce7f3,stroke:#ec4899,stroke-width:2px,color:#1e293b
    classDef production fill:#d1fae5,stroke:#10b981,stroke-width:2px,color:#1e293b
    classDef decision fill:#f3f4f6,stroke:#6b7280,stroke-width:2px,color:#1e293b,stroke-dasharray: 5 5
    
    class A,B,C foundation
    class D,E core
    class F decision
    class G,H,I specialization
    class J advanced
    class K,L,M,N production
      `} />
    </div>
    
    <div class="callout info">
      <div class="callout-title">💡 Pro Tip</div>
      <p>
        If you're new to AI, follow the chapters in order. If you have experience, feel free to jump to 
        specific topics using the sidebar navigation. Each chapter is self-contained but builds on previous concepts.
      </p>
    </div>
  </section>

  <!-- AI Studio Integration Placeholder -->
  <section class="ai-integration-placeholder">
    <h2>🤖 Future: AI-Powered Learning Assistant</h2>
    <div id="ai-chat-widget-placeholder" class="callout info">
      <p>
        This section is reserved for future integration with Google AI Studio or other AI chat interfaces.
        The placeholder ensures the site structure supports interactive AI assistance when implemented.
      </p>
    </div>
  </section>

  <section class="about-author">
    <h2>👨‍💻 About the Author</h2>
    <div class="author-card">
      <div class="author-content">
        <p>
          This comprehensive AI learning portal was created by <strong>Navdeep Singh Chander</strong>, 
          a passionate technologist dedicated to making AI education accessible and practical for everyone.
        </p>
        <p>
          With extensive experience in artificial intelligence, machine learning, and software engineering, 
          Navdeep brings real-world insights and hands-on expertise to every chapter of this guide.
        </p>
        <div class="author-links">
          <a href="https://nchand02.github.io/bio/" class="btn btn-primary" target="_blank" rel="noopener">
            Learn More About the Author →
          </a>
          <a href="https://github.com/nchand02" class="btn btn-secondary" target="_blank" rel="noopener">
            GitHub Profile
          </a>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .hero {
    text-align: center;
    padding: var(--space-4xl) var(--space-xl);
    margin-bottom: var(--space-4xl);
    position: relative;
    overflow: hidden;
  }
  
  .hero::before {
    content: '';
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(circle at center, rgba(99, 102, 241, 0.1) 0%, transparent 50%);
    animation: pulse-glow 8s ease-in-out infinite;
  }
  
  @keyframes pulse-glow {
    0%, 100% { transform: scale(1); opacity: 0.5; }
    50% { transform: scale(1.1); opacity: 0.8; }
  }
  
  .hero h1 {
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    margin-bottom: var(--space-xl);
    background: var(--gradient-primary);
    background-size: 200% 200%;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    animation: gradient-flow 6s ease infinite;
    font-weight: 800;
    letter-spacing: -0.03em;
    line-height: 1.1;
    position: relative;
    z-index: 1;
  }
  
  @keyframes gradient-flow {
    0%, 100% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
  }
  
  .hero-subtitle {
    font-size: clamp(1.125rem, 2vw, 1.5rem);
    color: var(--color-text-secondary);
    margin-bottom: var(--space-xl);
    font-weight: 600;
    position: relative;
    z-index: 1;
  }
  
  .hero-description {
    font-size: var(--font-size-lg);
    color: var(--color-text-tertiary);
    max-width: 750px;
    margin: 0 auto var(--space-2xl);
    line-height: var(--line-height-relaxed);
    position: relative;
    z-index: 1;
  }
  
  .cta-buttons {
    display: flex;
    gap: var(--space-lg);
    justify-content: center;
    flex-wrap: wrap;
    position: relative;
    z-index: 1;
  }
  
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-sm);
    padding: var(--space-lg) var(--space-2xl);
    border-radius: var(--radius-full);
    font-weight: 600;
    font-size: var(--font-size-lg);
    text-decoration: none;
    transition: all var(--transition-spring);
    position: relative;
    overflow: hidden;
    border: none;
  }
  
  .btn::before {
    content: '';
    position: absolute;
    inset: 0;
    background: var(--gradient-primary);
    transition: transform var(--transition-base);
  }
  
  .btn::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(255,255,255,0.2) 0%, transparent 100%);
    opacity: 0;
    transition: opacity var(--transition-base);
  }
  
  .btn-primary {
    background: var(--gradient-primary);
    color: white;
    box-shadow: var(--shadow-colored);
  }
  
  .btn-primary:hover {
    text-decoration: none;
    transform: translateY(-3px) scale(1.02);
    box-shadow: var(--shadow-glow), var(--shadow-xl);
  }
  
  .btn-primary:hover::after {
    opacity: 1;
  }
  
  .btn-primary:active {
    transform: translateY(-1px) scale(0.98);
  }
  
  .btn-secondary {
    background: var(--glass-bg);
    backdrop-filter: blur(10px);
    color: var(--color-text-primary);
    border: 2px solid var(--color-border);
  }
  
  .btn-secondary::before {
    background: var(--gradient-primary);
    opacity: 0;
  }
  
  .btn-secondary:hover {
    border-color: transparent;
    color: white;
    text-decoration: none;
    transform: translateY(-3px) scale(1.02);
    box-shadow: var(--shadow-colored);
  }
  
  .btn-secondary:hover::before {
    opacity: 1;
  }
  
  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: var(--space-xl);
    margin-bottom: var(--space-4xl);
  }
  
  .feature-card {
    padding: var(--space-2xl);
    background: var(--glass-bg);
    backdrop-filter: blur(10px) saturate(180%);
    -webkit-backdrop-filter: blur(10px) saturate(180%);
    border: 1px solid var(--glass-border);
    border-radius: var(--radius-xl);
    transition: all var(--transition-spring);
    position: relative;
    overflow: hidden;
  }
  
  .feature-card::before {
    content: '';
    position: absolute;
    inset: 0;
    background: var(--gradient-primary);
    opacity: 0;
    transition: opacity var(--transition-base);
  }
  
  .feature-card:hover {
    border-color: rgba(99, 102, 241, 0.3);
    box-shadow: var(--shadow-2xl), var(--shadow-glow);
    transform: translateY(-8px) scale(1.02);
  }
  
  .feature-card:hover::before {
    opacity: 0.05;
  }
  
  .feature-icon {
    font-size: 3rem;
    margin-bottom: var(--space-lg);
    display: inline-block;
    transition: transform var(--transition-bounce);
    filter: drop-shadow(0 4px 12px rgba(99, 102, 241, 0.3));
  }
  
  .feature-card:hover .feature-icon {
    transform: scale(1.2) rotate(5deg);
  }
  
  .feature-card h3 {
    margin-top: 0;
    margin-bottom: var(--space-md);
    font-size: var(--font-size-xl);
    color: var(--color-text-primary);
    font-weight: 700;
  }
  
  .feature-card p {
    margin-bottom: 0;
    color: var(--color-text-secondary);
    font-size: var(--font-size-base);
    line-height: var(--line-height-relaxed);
  }
  
  .chapter-overview {
    margin-top: var(--space-4xl);
  }
  
  .chapter-overview h2 {
    text-align: center;
    margin-bottom: var(--space-3xl);
  }
  
  .chapter-list {
    display: flex;
    flex-direction: column;
    gap: var(--space-xl);
  }
  
  .chapter-item {
    display: flex;
    gap: var(--space-2xl);
    padding: var(--space-2xl);
    background: var(--glass-bg);
    backdrop-filter: blur(10px) saturate(180%);
    -webkit-backdrop-filter: blur(10px) saturate(180%);
    border: 1px solid var(--glass-border);
    border-radius: var(--radius-xl);
    transition: all var(--transition-spring);
    position: relative;
    overflow: hidden;
  }
  
  .chapter-item::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0;
    bottom: 0;
    width: 4px;
    background: var(--gradient-primary);
    transform: scaleY(0);
    transition: transform var(--transition-base);
  }
  
  .chapter-item:hover {
    border-color: rgba(99, 102, 241, 0.3);
    box-shadow: var(--shadow-xl), var(--shadow-glow);
    transform: translateX(8px);
  }
  
  .chapter-item:hover::before {
    transform: scaleY(1);
  }
  
  .chapter-num {
    font-size: var(--font-size-4xl);
    font-weight: 800;
    background: var(--gradient-primary);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    min-width: 80px;
    line-height: 1;
    transition: transform var(--transition-bounce);
  }
  
  .chapter-item:hover .chapter-num {
    transform: scale(1.1);
  }
  
  .chapter-info {
    flex: 1;
  }
  
  .chapter-info h3 {
    margin-top: 0;
    margin-bottom: var(--space-md);
    font-size: var(--font-size-2xl);
    color: var(--color-text-primary);
    font-weight: 700;
  }
  
  .chapter-info p {
    margin-bottom: var(--space-lg);
    color: var(--color-text-secondary);
    line-height: var(--line-height-relaxed);
  }
  
  .chapter-link {
    display: inline-flex;
    align-items: center;
    gap: var(--space-sm);
    color: var(--color-primary);
    font-weight: 600;
    text-decoration: none;
    transition: all var(--transition-base);
    padding: var(--space-sm) var(--space-md);
    border-radius: var(--radius-full);
    background: rgba(99, 102, 241, 0.1);
  }
  
  .chapter-link:hover {
    text-decoration: none;
    background: var(--gradient-primary);
    color: white;
    transform: translateX(4px);
    box-shadow: var(--shadow-colored);
  }
  
  .learning-path {
    margin-top: var(--space-3xl);
    padding-top: var(--space-3xl);
    border-top: 1px solid var(--color-border);
  }
  
  .ai-integration-placeholder {
    margin-top: var(--space-3xl);
    padding-top: var(--space-3xl);
    border-top: 1px solid var(--color-border);
  }
  
  .about-author {
    margin-top: var(--space-3xl);
    padding-top: var(--space-3xl);
    border-top: 1px solid var(--color-border);
  }
  
  .author-card {
    padding: var(--space-2xl);
    background: linear-gradient(135deg, var(--color-surface) 0%, var(--color-background) 100%);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-md);
  }
  
  .author-content p {
    font-size: var(--font-size-md);
    line-height: var(--line-height-relaxed);
    color: var(--color-text-secondary);
    margin-bottom: var(--space-lg);
  }
  
  .author-content strong {
    color: var(--color-text);
    font-weight: 600;
  }
  
  .author-links {
    display: flex;
    gap: var(--space-lg);
    margin-top: var(--space-xl);
    flex-wrap: wrap;
  }
  
  @media (max-width: 768px) {
    .hero h1 {
      font-size: var(--font-size-3xl);
    }
    
    .chapter-item {
      flex-direction: column;
      gap: var(--space-md);
    }
    
    .chapter-num {
      font-size: var(--font-size-2xl);
    }
    
    .author-links {
      flex-direction: column;
    }
    
    .author-links .btn {
      width: 100%;
      text-align: center;
    }
  }
</style>
